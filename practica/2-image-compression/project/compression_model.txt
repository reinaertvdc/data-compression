HEADER



16		unsigned	width (in blocks)

16		unsigned	height (in blocks)

1		unsigned	rle

4		unsigned	m = amount of bits used per quantization matrix value

m*16	unsigned	quantization matrix values



BLOCK



(if rle)
4		unsigned	n = amount of non-zero values

4		unsigned	b = amount of bits used per value

16		signed		values offset

b*n		signed		values
